services:

  backend:
    build: ./backend
    container_name: hospital-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: hospital-frontend
    ports:
      - "5173:5173"